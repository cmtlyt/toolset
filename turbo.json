{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build$"],
      "outputs": ["dist/**"]
    },
    "deploy": {
      "dependsOn": ["^deploy$"],
      "outputs": ["dist/**"]
    },
    "publish": {
      "dependsOn": ["^publish$"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^dev$"],
      "cache": false,
      "persistent": true
    },
    "dev:doc": {
      "dependsOn": ["^dev:doc$"],
      "cache": false,
      "persistent": true
    },
    "build:doc": {
      "dependsOn": ["^build:doc$"],
      "cache": false,
      "persistent": true
    }
  }
}
