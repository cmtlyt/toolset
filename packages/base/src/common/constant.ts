export const INTERNAL_EMPTY = (Symbol && Symbol('internal-empty')) || {};

export const EMPTY = (Symbol && Symbol('empty')) || {};

export const STATIC_TYPE = ['url', 'map', 'set', 'weakmap', 'weakset'];
